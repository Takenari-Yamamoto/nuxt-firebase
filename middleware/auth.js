export default function ({ store, redirect }) {
  // store.dispatch('user/onAuth')
  // store.dispatch('user/nuxtServerInit')
  console.log('middleware')

  // if (!store.state.user.isLoggedIn) {
  //   return redirect ('/')
  // }

  // console.log(store.state.user.user)
}
